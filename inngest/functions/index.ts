/**
 * Every durable function in the ledger. The bodies are plain async functions
 * over the Step interface in lib/runtime/step.ts, registered here for Inngest
 * Cloud and run by the local dispatcher when no Inngest keys are configured.
 */
export const functions: [] = [];
